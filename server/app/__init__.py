"""AWR Fleet Server -- orchestration app package.

Pure stdlib, Python 3.9-compatible. This package never edits or shells
into anything but the existing generator scripts (run_awr_fleet.sh /
run_awr_trend.sh) at the repo root; see CLAUDE.md "Fleet report" section
for the cardinal no-touch rule this whole tree respects.
"""
